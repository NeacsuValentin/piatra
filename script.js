console.log('Start Game')

var possibilities = ["rock","paper","scissors"];

const user = possibilities[Math.floor(Math.random()*possibilities.length)]

const computer = possibilities[Math.floor(Math.random()*possibilities.length)]

console.log({user,computer});

function getWinner(user,computer) {
    console.log({user,computer});
    let winner;
    if (userChoice ===computerChoise) {
        winner = 'draw';
        var test ="2"
    } else {-
    }
    console.log()
    return winner

}


console.log(getWinner(user,computer));
console.log("ceva");
